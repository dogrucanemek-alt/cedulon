/** Single source of protocol identity. Rename must be one commit. */
export const PROTOCOL_SHORT = "ATP" as const;
export const PROTOCOL_LONG = "Agent Trade Protocol" as const;
export const PACKAGE_SCOPE = "@agent-trade-protocol" as const;
