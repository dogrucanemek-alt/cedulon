/** Single source of protocol identity. Rename must be one commit. */
export const PROTOCOL_SHORT = "Cedulon" as const;
export const PROTOCOL_LONG = "Cedulon Protocol" as const;
export const PACKAGE_SCOPE = "@cedulon" as const;
