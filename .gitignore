node_modules/
dist/
*.tgz

# npm run mcpb staging and the .mcpb it produces (a release artifact, not source)
build/
coverage/
*.log
.env
.write-key
.DS_Store
.turbo
spec/author-tools-*-response.bin
spec/idnits-*.bin
spec/idnits-out.txt

# kramdown-rfc reference cache
spec/.refcache/

# kramdown-rfc cache (repo root, when run from there)
.refcache/

# kramdown-rfc HTML render (not the submitted artifact)
spec/*.html

# Throwaway probes from adversarial review rounds. They measure one thing and
# are deleted once the finding becomes a real test; twice now they have been
# swept into a commit by `git add tests`.
tests/_probe_*
