# Submitting draft-dogru-cedulon-00 (individual Internet-Draft)

This file is a preparation note. It is not a submission.

**Patron approval is required before any Datatracker upload, email to
the Secretariat, or announcement.** Cursor and Claude must not submit.

## Intended metadata (from the markdown source)

| Field | Value |
|---|---|
| Filename / docname | `draft-dogru-cedulon-00` |
| Title | Cedulon: An Audit Layer for Agent-to-Agent Commerce |
| Abbreviation | Cedulon |
| Intended status / category | Informational (`info`) |
| IPR | `trust200902` (IETF Trust Legal Provisions) |
| Suggested area | `sec` |
| Working group | none (individual submission; not a WG item) |
| Date in source | 2026-08-25 |
| Author name | Emek Can Dogru |
| Author initials | E. C. Dogru |
| Affiliation | Independent |
| Contact | add email on Datatracker only after patron approval |

## Tooling status — DOGRULANAMADI

Attempted on 2026-08-25. Raw transcript: `spec/kramdown-rfc-attempt.txt`.

| Tool | Present | Usable | Output |
|---|---|---|---|
| `kramdown-rfc` 1.7.39 | yes (`C:\Ruby33-x64\bin`) | no — WDAC blocked `stringio.so` | no `.xml` |
| `xml2rfc` 3.34.0 | yes | yes, but needs RFCXML input | no `.txt` |
| `idnits` | no | n/a | not run |

Therefore `spec/draft-dogru-cedulon-00.txt` and `.xml` **do not exist**.
Do not invent them. After a machine that can run `kramdown-rfc` (or an
equivalent markdown-to-rfcxml converter) is available:

```
kramdown-rfc spec/draft-dogru-cedulon-00.md > spec/draft-dogru-cedulon-00.xml
xml2rfc --v3 --text --html spec/draft-dogru-cedulon-00.xml
```

Then compare the rendered text to the markdown source before submit.

## Manual idnits-like checklist (source markdown)

Checked against `spec/draft-dogru-cedulon-00.md` on 2026-08-25. These
are human checks, not a substitute for xml2rfc + idnits.

| Check | Result |
|---|---|
| Filename matches `draft-<name>-<nn>` | pass — `draft-dogru-cedulon-00` |
| Title, abstract, introduction present | pass |
| Abstract length | pass — about 115 words (under the usual ~20-line / short-abstract guidance) |
| Informational category + `ipr: trust200902` | pass in YAML front matter |
| Author is Emek Can Dogru / Independent | pass |
| BCP 14 hook | present as `{::boilerplate bcp14-tagged}` (expands only when kramdown-rfc runs) |
| Security Considerations | pass (T1–T11 restated) |
| IANA Considerations | pass (explicit: no IANA actions in -00) |
| Acknowledgments unnumbered | present |
| Normative refs listed | RFC2119, RFC8174, RFC8392, RFC8949, RFC9052, RFC9942, RFC9943 |
| Informative refs listed | RFC9110, RFC9421, SHARIF-ATTP, BATES-ATTP |
| Internal section anchors `cose-profile`, `reconciliation`, `escrow-role`, `privacy` | pass (`{#privacy}` added 2026-08-25 so `{{privacy}}` resolves) |
| Threat-id xrefs (`{{MUST-T1-1}}` … `{{MAY-T11-6}}`) | **open** — not defined as kramdown references or section anchors; xml generation will warn or fail until they are mapped (companion `THREAT_MODEL.md` is the intended table, not an I-D reference) |
| Source line length ≤ 72 | **open** — 56 of 461 source lines exceed 72 (longest 87); xml2rfc wrapping would fix the `.txt` form, not verified |
| Status of This Memo / Copyright Notice | **DOGRULANAMADI** — generated by xml2rfc from `ipr`; not present as literal text in the markdown |
| Authors' Addresses block | **DOGRULANAMADI** — generated; source has name/org only, no postal or email (add only with patron approval) |
| idnits run | **DOGRULANAMADI** — binary not installed |
| Content match of `.txt`/`.xml` to markdown | **DOGRULANAMADI** — files not generated |

## Individual submission steps (do not run until patron says so)

Sources: [authors.ietf.org submitting](https://authors.ietf.org/submitting-your-internet-draft),
[Datatracker submit tool](https://datatracker.ietf.org/submit/),
[I-D guidelines](https://ietf.github.io/id-guidelines/).

1. Generate RFCXML v3 and a text rendering on an unblocked machine.
   Fix unresolved `{{MUST-T…}}` xrefs first so xml2rfc is clean.
2. Create or use an IETF Datatracker account for Emek Can Dogru.
   Confirm the account email.
3. Open `https://datatracker.ietf.org/submit/` while logged in.
4. Upload the `.xml` (preferred authoritative form). The tool will
   generate text and HTML. A lone `.txt` is accepted only if XML cannot
   be produced.
5. Confirm the parsed metadata matches the table above
   (name, informational, author).
6. **Stop and wait for patron approval** if it has not already been
   given in writing for this exact file hash.
7. After approval only: click through posting. For a `-00`, Datatracker
   emails the listed authors unless the submitter is a logged-in author.
8. Do not mail `iesg@`, do not request a working group, do not claim
   IETF consensus. This remains an individual informational draft.

Blackout: I-Ds are refused between the pre-meeting cutoff and the
session start. Check `https://www.ietf.org/how/meetings/` dates before
any approved submit.

## Out of scope for this repository pass

- No Datatracker upload.
- No author email added to the draft.
- No DNS, GitHub, or Vercel action.
